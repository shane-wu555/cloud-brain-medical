# 智慧云脑诊疗平台技术选型

## 一、总体架构

平台采用 **前后端分离 + 微服务架构 + Spring AI 业务智能编排 + FastAPI 高级 AI 服务层 + 医疗数据基础设施** 的分层架构。整体技术路线以《智慧云脑诊疗平台.md》中“挂号—收费—门诊接诊—检查/检验/处置—报告录入—确诊—处方—药房发药”的门诊 HIS 主流程为基准，同时保留患者端 AI 智能问诊、AI 医生推荐、AI 分诊台、AI 报告分析、AI 助理医生和 AI 排班能力。

1. **用户交互层**
   - 患者微信小程序：注册登录、实名认证、AI 智能问诊、线上挂号、缴费、报告查看、病历查看、取药状态查看。
   - 医院工作人员 PC Web：按照权限提供门诊医生、检查医生、检验医生、处置医生、药房医生、窗口挂号收费人员和管理员工作台；放射科/影像医生职责归入检查医生。
   - 管理端：医生账号、科室、排班、号源、药品、项目、权限、日志、AI 服务监控与数据看板。
   - 窗口挂号收费工作台：线下挂号、退号、收费、退费、发票、日结，结果实时同步至统一业务系统。
   - 药房医生工作台：处方接收、发药、退药、库存扣减与交易记录。

2. **接入网关层**
   - API Gateway / Nginx。
   - 统一鉴权、限流、路由、灰度发布、跨端访问控制。
   - 对患者微信小程序和医院工作人员 PC Web 进行统一 API 接入。

3. **业务微服务层**
   - Spring Boot 3.x + Java 17 微服务：
     - auth-service（认证、短信验证码、实名认证、权限）
     - patient-service（患者信息、健康档案）
     - doctor-service（医生信息、科室、排班、调班、停诊和医生擅长方向）
     - appointment-service（线上/线下挂号、排队、号源、退号）
     - cashier-service（收费、退费、发票、日结）
     - medical-record-service（电子病历、病历首页、诊断记录）
     - medical-order-service（检查、检验、处置、处方医嘱统一管理）
     - pharmacy-service（处方接收、发药、退药、库存）
     - report-service（检查/检验/影像报告、AI 报告草稿、正式报告）
     - audit-service（医疗数据访问、AI 调用、关键操作审计）
   - 每个服务可采用独立 schema，关键业务通过事件驱动和事务消息保证状态一致性。

4. **Spring AI 业务智能编排层**
   - 在 Spring Boot 业务服务内部引入 **Spring AI**，用于与 Java 业务流程强绑定、响应时延要求较低、规则边界清晰的 AI 能力：
     - 患者端简单问答入口与提示词模板管理；
     - AI 问诊结果摘要的结构化解析与业务字段落库；
     - 医生端病历摘要、检查检验结果摘要、处方风险提示的轻量调用；
     - 管理端排班建议的提示词封装和结果结构化；
     - AI 调用日志、模型路由、Prompt Template、Output Parser、Advisor/RAG 调用封装；
     - 作为业务微服务到 Python AI 服务的 AI Gateway，统一鉴权、限流、审计和降级。
   - Spring AI 不替代高级 AI 服务，而是作为 Java 业务侧 AI 接入与治理框架，保证 AI 能力能自然嵌入挂号、病历、检查、处方、排班等业务流程。

5. **高级 AI 智能服务层**
   - 仍然采用 **FastAPI + LangChain + LangGraph + PostgreSQL/pgvector** 作为关键 AI 能力的主实现。
   - 适用范围：
     - 患者端多轮 AI 智能问诊；
     - 基于症状、病历、医生擅长方向和号源的医生推荐；
     - AI 分诊台检查资源调度；
     - AI 助理医生诊断建议、进一步检查建议、用药建议；
     - AI 智能检查报告草稿生成；
     - 头部 CT / DICOM 影像识别与异常区域提示；
     - 医学知识库 RAG、长期上下文管理、多工具调用、多步骤 Agent 流程。
   - 采用 SSE/流式输出支持 AI 问诊和医生端辅助书写；采用异步任务处理影像识别、报告生成等长耗时任务。
   - 所有 AI 输出均需要保留输入、输出、模型版本、提示词版本、医生确认状态和人工修正记录。

6. **数据与基础设施层**
   - PostgreSQL：核心 HIS 业务数据库，保存患者、挂号、收费、病历、医嘱、报告、处方、药品和排班等事务数据。
   - pgvector：PostgreSQL 的向量扩展，用于医学知识库、症状向量、医生擅长方向和病历片段检索。核心业务表与向量表采用独立 schema 和权限隔离。
   - Redis：Token、短信验证码、号源锁定、队列缓存、AI 会话短期上下文、限流。
   - RabbitMQ：挂号成功、缴费成功、检查完成、报告确认、处方确认、发药完成、AI 异步任务等事件。
   - MinIO：检查报告、检验附件、影像文件、DICOM、PDF 报告。
   - Elasticsearch：病历、报告、药品、疾病、医生擅长方向全文检索。
   - Prometheus + Grafana + OpenTelemetry + Jaeger：监控、链路追踪、AI 服务耗时与失败率观测。

---

## 二、前端技术选型

患者端与医院工作人员端采用两套前端工程，共享后端 API、数据模型和业务状态，不共享终端页面。

### 2.1 患者微信小程序

| 技术 | 说明 |
|---|---|
| 框架 | uni-app + Vue 3，编译发布为微信小程序 |
| 语言 | TypeScript |
| UI 组件 | uni-ui / Wot Design Uni |
| 状态管理 | Pinia |
| 网络请求 | uni.request 统一封装，接入登录态、错误处理和接口签名 |
| 微信能力 | 微信登录、手机号授权、订阅消息、微信支付 |

### 2.2 医院工作人员 PC Web

| 技术 | 说明 |
|---|---|
| 框架 | Vue 3 + Composition API |
| 语言 | TypeScript |
| UI组件 | Element Plus / Naive UI |
| 状态管理 | Pinia |
| 路由管理 | Vue Router（动态路由 + 权限控制） |
| HTTP请求 | Axios（统一封装 + Token拦截） |
| 样式 | SCSS + UnoCSS / Tailwind CSS |
| 富文本编辑 | WangEditor 5 / TipTap（病历首页、报告编辑、处方医嘱） |
| 图表可视化 | ECharts（排班、号源、检查队列、药房库存、AI 服务监控） |
| 文件与影像 | pdfjs-dist、Viewer.js、cornerstone.js（报告、影像、DICOM 预览） |
| 实时通信 | WebSocket（叫号、队列、药房状态）、SSE（AI 流式输出） |

**前端升级点：**

- 工作台模式：门诊医生端高信息密度，检查/检验/处置医生端突出申请队列与结果录入，药房医生端突出处方和库存，管理端突出数据驾驶舱。
- 动态权限控制：页面、菜单、按钮、数据范围均与角色绑定。
- AI 模块独立组件：问诊、医生推荐、分诊台、报告草稿、辅助诊断、处方风险提醒、排班建议。
- 队列与状态可视化：门诊队列、检查队列、检验队列、处置队列、药房待发药/已发药/已退药状态。

---

## 三、后端技术选型（微服务 + Spring AI + 高级 AI 闭环）

| 模块 | 技术/说明 |
|---|---|
| 主业务微服务 | Spring Boot 3.x + Java 17 |
| 数据访问 | MyBatis / MyBatis-Plus，与主文档既有 MyBatis 技术基线保持一致 |
| 构建工具 | Maven |
| Java 侧 AI 编排 | Spring AI，用于 Prompt 模板、轻量 RAG、结构化输出、模型路由、AI 调用审计和业务嵌入 |
| 高级 AI 服务 | FastAPI + LangChain + LangGraph + PyTorch + Transformers + MONAI |
| 核心业务数据库 | PostgreSQL（保证挂号、缴费、病历等业务事务一致性） |
| 向量检索 | PostgreSQL + pgvector（医学知识库、病历片段向量、医生擅长方向和症状语义索引） |
| 缓存 | Redis（Token、验证码、号源锁定、AI上下文、限流、队列） |
| 权限认证 | Spring Security + JWT + RBAC，支持患者、窗口人员、门诊/检查/检验/处置/药房医生和管理员 |
| 异步消息 | RabbitMQ（事件驱动、AI任务、通知、状态流转） |
| 文件存储 | MinIO（报告、影像、附件、DICOM） |
| 搜索 | Elasticsearch（病历/报告/药品/疾病/医生全文检索） |
| 实时通信 | WebSocket（队列叫号、药房状态）、SSE（AI流式输出） |
| 服务治理 | Nacos（服务注册、配置中心） |
| 限流与熔断 | Sentinel / Resilience4j，AI 服务失败时支持规则兜底 |
| 日志与监控 | Spring Boot Actuator + Prometheus + Grafana + OpenTelemetry + Jaeger |
| 定时任务 | Spring Scheduler / XXL-JOB（号源释放、日结、排班同步、过期任务清理） |
| 部署 | Docker Compose（开发） / Kubernetes（生产），AI 服务可单独 GPU 部署 |

**Spring AI 与 FastAPI + LangChain 的边界：**

| 场景 | 推荐技术 | 原因 |
|---|---|---|
| 患者端多轮问诊、追问策略、风险分层 | FastAPI + LangChain/LangGraph + PostgreSQL/pgvector | 需要复杂上下文、多轮状态机、RAG 和安全控制 |
| AI 助理医生综合病历、报告、知识库给出诊断/用药建议 | FastAPI + LangChain/LangGraph + PostgreSQL/pgvector | 属于高风险复杂 AI 决策，需可追溯、多工具、多模型编排 |
| 头部 CT 影像识别 | FastAPI + PyTorch/MONAI + MinIO/PostgreSQL | 涉及医学影像模型与 GPU 推理，影像文件存 MinIO，业务元数据存 PostgreSQL |
| AI 分诊台资源调度 | FastAPI + 优化算法/规则引擎 + PostgreSQL/Redis | 需要结合队列、设备状态、急诊优先级进行调度 |
| 病历摘要、报告摘要、处方风险提示 | Spring AI 或 Spring AI 调用 FastAPI | 与 Java 业务表单强绑定，适合在业务服务内封装 |
| 排班方案草稿、提示词模板、AI 结果结构化落库 | Spring AI | 与管理端业务流和权限审计关系密切 |
| AI 调用日志、提示词版本、模型路由、降级策略 | Spring AI + audit-service | 便于 Java 微服务统一治理 |

---

## 四、前后端模块划分

### 患者微信小程序
- 手机号注册、短信验证码、8 位数字+字母密码校验。
- 手机号+密码登录、手机号+验证码登录、忘记密码。
- 实名认证。
- AI 智能问诊：症状标签勾选并填写持续时间、文本补充、多轮追问、风险等级、就诊建议。
- AI 推荐科室与 3-5 名医生，也支持自主搜索科室和医生。
- 线上挂号：科室、医生、日期、时间段、号源锁定、缴费、取消挂号与退费。
- 电子病历查看：本次病历、历史病历、检查检验报告、处方、取药记录。
- 检查/检验/处置缴费、报告查看、药房取药状态查看。

### 医生端
医生端不再只抽象为“医生”，而是按照业务岗位拆分：

1. **门诊医生**
   - 今日接诊队列，包含线上挂号和线下窗口挂号患者。
   - 创建/编辑病历首页，查看 AI 问诊记录和历史相关病历。
   - 开立检查、检验、处置申请。
   - 查看检查/检验/影像正式报告。
   - 门诊确诊、开立处方、医嘱、就诊归档。
   - 使用 AI 助理医生进行病情摘要、诊断建议、检查建议、用药建议和病历书写辅助。

2. **检查医生**
   - 查看检查申请队列。
   - 患者检查登记、科室/医生分配、开始检查。
   - 录入检查结果，提交检查报告。
   - 对 AI 报告草稿进行确认、修改或驳回。
   - 承担放射科/影像医生职责，处理 CT 等影像检查，查看 AI 识别结果并审核、修正、确认正式影像报告。

3. **检验医生**
   - 查看检验申请队列。
   - 标本/检验项目登记。
   - 录入检验结果，提交检验报告。
   - 对异常指标和 AI 摘要进行复核。

4. **处置医生**
   - 查看处置申请队列。
   - 执行处置项目，例如洗胃、换药、治疗性操作等。
   - 录入处置结果和执行记录。

5. **药房医生**
   - 接收医生确认且患者已缴费的处方。
   - 发药、退药、库存扣减与回补，并维护药品交易记录。

### 窗口挂号收费工作台（PC Web）
- 现场挂号、退号。
- 挂号费、检查费、检验费、处置费、处方费收费。
- 退费、发票重打/补打、收费日结。
- 线下挂号患者进入统一门诊队列。
- 挂号、缴费、退号和退费结果实时写入统一业务系统，并同步影响号源、队列、医嘱执行状态和电子病历。

### 药房医生工作台（PC Web）
- 接收医生确认后的处方。
- 查询待发药、已发药、已退药患者。
- 发药、退药、库存扣减、库存回补。
- 药品交易记录查看。

### 管理端
- 医生账号创建与医生类型维护。
- 科室、挂号级别、结算类别、医技项目、药品、疾病、排班维护。
- 医生排班、调班、停诊、请假、调休、出差管理。
- AI 辅助排班并由管理员确认。
- 号源同步、系统日志、AI 服务监控、数据看板。

### AI智能模块（红色标识）
- AI智能问诊（患者端）：辅助症状初筛、风险分级、就诊建议。
- AI医生推荐（患者端）：推荐科室和 3-5 名医生。
- AI分诊台（检查/检验/处置调度）：优化检查地点、时间和队列。
- AI智能检查报告：生成报告草稿，必须由检查医生或检验医生按项目类型确认。
- 头部CT影像识别：异常区域检测和风险提示，必须由检查医生确认。
- AI助理医生（门诊医生端）：辅助诊断、辅助开药、病历生成。
- AI医生排班（管理端）：辅助生成排班方案，必须由管理员确认。

---

## 五、数据库表设计建议

### 1. 用户权限类
- sys_user / sys_role / sys_permission / sys_user_role / sys_role_permission / sys_login_log。
- sys_org / sys_dept / sys_user_profile。
- user_real_name_auth（患者实名认证）。
- sms_code_log（短信验证码与找回密码记录）。

### 2. 医疗业务类
- patient / doctor / department / doctor_schedule / appointment / register / visit_record。
- doctor_type（医生类型：门诊医生、检查医生、检验医生、处置医生、药房医生）。
- cashier_order / payment_record / refund_record / invoice_record / daily_settlement。
- medical_record / medical_record_disease / diagnosis / medical_order。
- check_request / inspection_request / disposal_request。
- inspection_report / laboratory_report / disposal_record / imaging_report。
- prescription / prescription_item / drug / drug_inventory / drug_transaction。
- medical_technology（检查、检验、处置等医技项目）。
- queue_record / queue_call_log / missed_queue_record（门诊、检查、检验、处置队列与过号记录）。

### 3. AI智能类
- ai_session / ai_message（患者问诊会话、多轮追问）。
- ai_symptom_tag / ai_consultation_summary / ai_triage_result。
- ai_doctor_recommendation（科室与医生推荐结果）。
- ai_queue_dispatch（AI 分诊台调度记录）。
- ai_report_analysis / ai_image_analysis。
- ai_doctor_assist_result（辅助诊断、用药建议、病历草稿）。
- ai_scheduling（AI 医生排班生成）。
- ai_model_registry / ai_prompt_template / ai_tool_call_log。
- ai_rag_document / ai_rag_chunk / ai_feedback / ai_evaluation_record。

### 4. 文件资源类
- file_resource / file_access_log。
- image_annotation / dicom_study / dicom_series / dicom_instance。
- report_attachment / medical_record_attachment。

### 5. 审计运维类
- operation_log / data_access_log / api_call_log / notification / message_outbox。
- ai_audit_log（AI 输入输出、医生确认、人工修正、模型版本）。
- sensitive_data_access_log（病历、报告、影像访问审计）。

---

## 六、开发顺序建议（分阶段）

**V1：基础 HIS 闭环**
- 前端框架、动态路由、权限控制。
- 患者注册登录、短信验证码、实名认证、找回密码。
- 医生账号、医生分类、科室、排班、号源。
- 线上挂号、线下窗口挂号、收费、退号、退费。
- 门诊医生接诊、病历首页保存。

**V2：检查/检验/处置与药房闭环**
- 门诊医生开立检查、检验、处置申请。
- 患者缴费后进入检查/检验/处置队列。
- 检查医生、检验医生、处置医生工作台。
- 检查/检验结果录入与正式报告。
- 门诊医生查看报告、确诊、开立处方。
- 药房发药、退药、库存变更和交易记录。

**V3：Spring AI 业务嵌入**
- 在 Spring Boot 微服务内接入 Spring AI。
- 病历摘要、报告摘要、处方风险提示、排班建议等轻量 AI 功能。
- Prompt 模板管理、结构化输出、模型路由、AI 调用审计。
- Spring AI 作为业务侧 AI Gateway 调用 FastAPI 高级 AI 服务。

**V4：高级 AI 智能能力**
- FastAPI AI 服务接入。
- LangChain / LangGraph 多轮问诊。
- AI 推荐科室与医生。
- AI 分诊台调度检查资源。
- AI 助理医生辅助诊断、辅助开药、病历生成。
- AI 检查报告、头部 CT 影像识别、流式输出（SSE）。
- AI 反馈、医生确认、人工修正记录。

**V5：高级影像与排班**
- CT 影像上传、DICOM 管理、MONAI 识别。
- cornerstone.js 影像预览。
- AI 医生排班生成、管理员复核、号源同步。
- 医生对 AI 报告和 AI 诊断结果的复核闭环。

**V6：微服务治理与部署**
- Nacos 配置与注册。
- Gateway 统一入口。
- RabbitMQ 事件驱动。
- Prometheus/Grafana + OpenTelemetry/Jaeger 监控。
- Kubernetes 生产部署，AI 服务 GPU 独立扩缩容。

---

## 七、部署方式

- **开发环境**：Docker Compose，包含 PostgreSQL/pgvector、Redis、RabbitMQ、MinIO、Elasticsearch、前端、后端、AI 服务。
- **测试环境**：Docker Compose + Nginx 反向代理 + 独立 AI 服务容器。
- **生产环境**：Kubernetes。
  - Gateway + Nginx。
  - Spring Boot 微服务弹性伸缩。
  - FastAPI AI 服务 GPU 独立部署。
  - PostgreSQL 主从或云数据库承载核心业务，启用 pgvector 扩展并对向量索引独立优化。
  - MinIO 分布式存储。
  - 日志收集 + Prometheus/Grafana + OpenTelemetry/Jaeger。

---

## 八、技术架构总结

**前端**：患者端采用 uni-app + Vue 3 + TypeScript 开发微信小程序；医院工作人员端采用 Vue 3 + TypeScript + Pinia + Vue Router 开发 PC Web，并按窗口挂号收费人员、门诊医生、检查医生、检验医生、处置医生、药房医生和管理员进行动态权限控制。

**后端**：Spring Boot 微服务为核心业务系统，Spring AI 负责 Java 业务侧 AI 接入、提示词模板、结构化输出、轻量 RAG、调用审计和 AI Gateway；FastAPI + LangChain + LangGraph + PostgreSQL/pgvector 负责多轮问诊、AI 助理医生、AI 分诊台、影像识别和复杂 RAG 等关键 AI 能力。

**数据库与存储**：PostgreSQL 承载核心业务数据，并通过 pgvector 承载 AI 向量索引；核心业务表与向量表采用独立 schema 和权限隔离，配合 Redis、MinIO 和 Elasticsearch 进行缓存、文件及全文检索的分层存储。

**微服务治理**：Nacos、Gateway、限流熔断、事件驱动、链路追踪、AI 服务降级。

**监控与运维**：Prometheus/Grafana + OpenTelemetry/Jaeger + 日志审计 + 医疗数据访问审计。

**部署方式**：Docker Compose（开发/测试） / Kubernetes（生产），AI 服务可独立 GPU 扩缩容。

**开发策略**：先完成传统门诊 HIS 主流程，再逐步嵌入 Spring AI 轻量智能能力，最后接入 FastAPI + LangChain 高级 AI 服务，形成“基础业务可用、AI 能力可控、医生确认闭环完整”的智慧诊疗平台。

---

## 九、项目基本目录

```text
cloud-brain-medical/
├── patient-miniapp/              # 患者微信小程序（uni-app + Vue 3）
│   └── src/
│       ├── api/                  # 统一 API 封装
│       ├── pages/                # 问诊、挂号、缴费、病历、报告、取药状态
│       ├── components/
│       ├── stores/
│       └── manifest.json
├── frontend/                     # 医院工作人员 PC Web（Vue 3）
│   ├── public/
│   └── src/
│       ├── api/                  # API接口封装
│       ├── assets/               # 静态资源
│       ├── components/           # 可复用组件
│       │   ├── common/           # 公共UI组件
│       │   ├── medical/          # 医疗业务组件
│       │   ├── queue/            # 门诊/检查/检验/处置队列组件
│       │   └── ai/               # AI相关组件
│       ├── views/                # 页面
│       │   ├── cashier/          # 窗口挂号收费工作台
│       │   ├── outpatientDoctor/ # 门诊医生端
│       │   ├── checkDoctor/      # 检查医生端
│       │   ├── labDoctor/        # 检验医生端
│       │   ├── disposalDoctor/   # 处置医生端
│       │   ├── pharmacyDoctor/   # 药房医生工作台
│       │   └── admin/            # 管理端
│       ├── router/               # 路由管理
│       ├── store/                # Pinia状态管理
│       ├── utils/                # 工具函数
│       └── App.vue
├── backend/                      # Spring Boot 后端微服务集合
│   ├── auth-service/
│   ├── patient-service/
│   ├── doctor-service/
│   ├── appointment-service/
│   ├── cashier-service/
│   ├── medical-record-service/
│   ├── medical-order-service/
│   ├── pharmacy-service/
│   ├── report-service/
│   ├── spring-ai-gateway-service/ # Spring AI业务智能编排与AI调用治理
│   └── audit-service/
├── ai-service/                   # FastAPI高级AI服务
│   ├── app/
│   │   ├── consultation/         # 多轮AI问诊
│   │   ├── recommendation/       # 科室与医生推荐
│   │   ├── triage/               # AI分诊台与检查调度
│   │   ├── doctor_assistant/     # AI助理医生
│   │   ├── report_analysis/      # 报告分析与报告草稿
│   │   ├── image_analysis/       # CT/DICOM影像识别
│   │   ├── rag/                  # 医学知识库RAG
│   │   └── scheduling/           # AI排班
│   ├── services/                 # 模型推理、LangChain/LangGraph流程
│   ├── schemas/                  # Pydantic Schema
│   ├── utils/                    # 提示词、安全过滤、审计工具
│   ├── main.py
│   ├── requirements.txt
│   └── Dockerfile
├── docker/                       # Docker Compose / 部署配置
│   ├── docker-compose.yml
│   ├── nginx/
│   │   └── nginx.conf
│   └── env/
├── k8s/                          # Kubernetes 部署配置
│   ├── deployment/
│   ├── service/
│   ├── configmap/
│   └── ingress/
├── scripts/                      # 数据库初始化、迁移、工具脚本
│   ├── db_init.sh
│   ├── migrate.sh
│   ├── seed_data.py
│   └── ai_tools/
│       └── build_embeddings.py
├── docs/                         # 项目文档、设计文档
└── README.md
```
