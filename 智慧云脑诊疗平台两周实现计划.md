# 智慧云脑诊疗平台两周实现计划

## 1. 计划说明

### 1.1 实施目标

在两个自然周、共 10 个工作日内完成智慧云脑诊疗平台 MVP，实现以下完整业务闭环：

```text
患者微信小程序注册与实名认证
→ AI 智能问诊或自主选择科室、医生
→ 微信小程序线上挂号 / PC Web 窗口线下挂号
→ 缴费并生成本次电子病历
→ 门诊医生接诊
→ 开具检查、检验或处置申请
→ 缴费并进入对应执行队列
→ 检查、检验或处置医生执行并提交结果
→ 门诊医生复诊、确诊并开具处方
→ 患者缴费
→ 药房医生发药
→ 电子病历归档
```

AI 部分采用独立并行计划，在第 1 周的 5 个工作日内完成可部署的 AI 服务，第 2 周接入主业务系统并完成联调。

### 1.2 实施基线

本计划以以下文档为需求与技术基线：

- 《智慧云脑诊疗平台.md》：总体业务和传统门诊 HIS 主流程基线。
- 《智慧云脑诊疗平台需求说明.md》：角色、功能、流程和业务规则基线。
- 《智慧云脑诊疗平台技术选型.md》：前后端、数据库、AI 服务和部署技术基线。

### 1.3 已确定的统一口径

| 项目 | 最终方案 |
|---|---|
| 患者端 | 微信小程序，采用 uni-app + Vue 3 + TypeScript |
| 医院工作人员端 | PC Web，采用 Vue 3 + TypeScript + Element Plus |
| 后端 | Spring Boot + Spring Security + MyBatis/MyBatis-Plus |
| 核心数据库 | PostgreSQL |
| AI 向量检索 | PostgreSQL + pgvector |
| AI 服务 | FastAPI + LangChain/LangGraph + PyTorch/MONAI |
| 检查医生 | 包含放射科医生、影像医生的职责，不另设放射/影像医生角色 |
| 药房医生 | 医生角色的一种，负责发药、退药和库存 |
| 窗口挂号收费 | PC Web 工作台，线下挂号和缴费结果实时写入统一业务系统 |
| 核心业务场景 | 神经外科、神经内科与脑科，重点演示头部 CT 和神经系统诊疗 |
| 医生端交互 | 左侧 HIS 业务操作区 + 右侧常驻 AI 助理区 |

## 2. 实施范围和优先级

### 2.1 P0：两周内必须完成

1. 用户认证、实名认证和角色权限。
2. 科室、医生、排班和号源基础数据。
3. 微信小程序线上挂号、窗口线下挂号及统一号源。
4. 缴费、退号、退费和业务状态同步。
5. 门诊医生接诊与电子病历。
6. 检查、检验、处置申请、执行及结果回传。
7. 头部 CT 上传、AI 识别结果查看和检查医生确认。
8. AI 智能问诊、医生推荐、医生端辅助诊断和报告草稿。
9. 处方、药房发药、退药和库存变更。
10. 管理员排班、基础数据和账号权限管理。
11. 关键操作日志、AI 调用记录和人工确认记录。
12. 可重复部署的测试环境及完整演示数据。
13. LIS 检验指标纵向明细、样本全生命周期和 AI 医疗数据强制溯源。

### 2.2 P1：时间允许时完成

- 微信订阅消息提醒。
- 发票重打、补打和收费日结完整报表。
- 医生常用检查、检验、处置和处方模板。
- 更完整的药品库存预警。
- AI 排班建议和基础数据看板。
- DICOM 在线阅片的高级标注工具。

### 2.3 本期不纳入

- 真实医保结算与医保机构联调。
- 正式生产微信支付，可先使用微信支付沙箱或模拟支付适配器。
- CT 模型重新训练和临床级模型验证。
- 复杂 Kubernetes 生产集群及跨地域容灾。
- 完整医院硬件、PACS、LIS 和第三方 HIS 对接。
- 将所有业务拆成可独立部署的微服务；本期可先采用模块化单体，保留服务边界。

### 2.4 改造前代码评估与改造原则

本项目不是从零开发。下表记录结构调整前的审计结果，用于解释改造来源；患者端拆分、角色细分、收费服务、统一医技服务和医生排班持久化已经按本计划实施。

#### 现有代码可复用情况

| 现有部分 | 当前状态 | 处理策略 |
|---|---|---|
| `frontend/` Vue 3 工程 | 可以正常构建，但混入患者 PC 页面，角色只有患者/医生/管理员 | 保留为医院工作人员 PC Web，移除患者业务入口并细分工作人员角色 |
| 患者 PC 页面 | 已实现简单问诊、医生查询和挂号演示 | 业务逻辑和 API 类型可参考，页面迁移到新建的微信小程序工程，不继续作为 PC 端入口 |
| `backend/` Maven 多模块 | 14 个模块可以正常编译，但多数服务只有启动类 | 保留父工程和已有服务，按需求补齐缺失模块和业务实现 |
| `auth-service` | 已接入 PostgreSQL，但密码明文、开发 Token、Security 全部放行 | 必须重构为密码哈希、有效 Token、细分角色和真实接口授权 |
| `appointment-service` | 已有线上锁号、支付、取消、过号和病历调用 | 保留领域代码，修复并发、线下号源扣减、支付记录、退款和跨服务一致性 |
| `medical-record-service` | 已有 PostgreSQL 表和初始病历接口 | 保留并扩展病历版本、诊疗内容、归档和访问审计 |
| `doctor-service` | 只有演示性质科室、医生和排班接口 | 迁移到 PostgreSQL，拆清医生资料与排班号源职责 |
| 检查、药房、报告、排班、审计服务 | 基本为空壳 | 按 P0 流程逐步补齐，优先实现完整闭环而非后台大而全 |
| 检验、处置、收费、统一医嘱服务 | 当前缺失 | 新增模块或在明确边界下先合并实现，接口和数据模型必须独立 |
| `ai-service` | 只有健康检查接口，目录为占位 | 按 AI 五日计划实现，不依赖占位代码的业务正确性 |
| Docker Compose | PostgreSQL/pgvector、Redis、RabbitMQ、MinIO、Elasticsearch 已定义 | 保留并增加健康检查、持久化卷和服务环境变量 |

#### 已确认必须修复的问题

1. 患者功能不能继续放在医院工作人员 PC Web，需新建独立微信小程序工程。
2. `DOCTOR` 角色过于笼统，必须细分为门诊、检查、检验、处置和药房医生。
3. 密码不能明文存储，接口不能使用伪 Token，Spring Security 不能全部放行。
4. 线下挂号必须与线上挂号使用同一号源库存，目前线下流程未统一扣减号源。
5. JVM `synchronized` 不能保证多实例下的号源安全，需使用 PostgreSQL 行锁或原子 SQL，并增加数据库约束。
6. 使用“记录总数 + 1”生成业务 ID 存在并发冲突，应改为 UUID、数据库序列或独立业务编号生成器。
7. 跨服务创建病历失败不能静默忽略，需要事件表、重试和可观测的补偿状态。
8. 当前只有挂号表中的支付状态，缺少统一支付单、退款单和窗口收费记录。
9. 大部分服务没有测试；现有 Maven 构建成功仅代表能够编译，不代表业务正确。

#### 改造约束

- 每次修改必须先补测试或至少定义可执行验收场景，再替换原实现。
- 现有数据库结构通过新增 Flyway 迁移演进，不修改已经执行的 V1 迁移。
- 患者 PC 页面在微信小程序覆盖对应功能并通过构建后移除。
- 高风险改造按“认证权限 → 号源事务 → 支付状态 → 病历一致性 → 医技和药房”的顺序执行。
- 每天结束时保持主分支可编译，不能一次性进行不可验证的大规模重写。

## 3. 需求对标矩阵

| 需求编号 | 需求模块 | 对应角色 | 实现内容 | 计划时间 | 验收依据 |
|---|---|---|---|---|---|
| R-01 | 注册、登录、找回密码 | 患者 | 手机号验证码、密码登录、找回密码、微信登录态绑定 | D2-D3 | 患者可完成注册、登录和密码重置 |
| R-02 | 实名认证 | 患者 | 姓名、身份证号、性别、出生日期、联系电话及认证状态 | D3 | 未实名用户不能挂号、缴费和查看完整病历 |
| R-03 | 医院账号与 RBAC | 医生、窗口人员、管理员 | 管理员创建账号，按角色控制菜单、按钮和数据范围 | D2-D3 | 不同角色只能访问授权工作台和数据 |
| R-04 | 科室、医生与排班 | 管理员 | 科室、医生类型、出诊日期、时段、号源、停诊 | D3 | 排班发布后线上和窗口均能查询相同号源 |
| R-05 | AI 智能问诊 | 患者 | 症状标签、持续时间、文本描述、多轮追问、风险等级 | AI-D2，集成 D6 | 输出结构化摘要、风险等级和就诊建议 |
| R-06 | 医生推荐 | 患者 | 按病症匹配度、排班、剩余号源和职称推荐 3-5 名医生 | AI-D3，集成 D6 | 推荐结果仅包含可挂号医生并允许患者自主搜索 |
| R-07 | 线上挂号 | 患者 | 科室、医生、日期、时段、号源锁定、支付、取消挂号 | D4-D5 | 支付成功生成挂号和电子病历，失败释放号源 |
| R-08 | 线下挂号收费 | 窗口人员 | 建档、挂号、收费、退号、退费、费用查询 | D4-D5 | 线下结果实时同步统一号源、队列和支付状态 |
| R-09 | 门诊队列与过号 | 门诊医生 | 今日队列、叫号、接诊、顺延 3 个号 | D5-D6 | 线上和线下患者进入同一队列，过号规则正确 |
| R-10 | 电子病历 | 患者、门诊医生 | 创建、编辑、更新、访问、归档和访问审计 | D5-D10 | 挂号后创建病历，诊疗节点持续更新并可归档 |
| R-11 | 门诊接诊 | 门诊医生 | 主诉、现病史、既往史、过敏史、体格检查、初步诊断 | D6 | 医生可以保存、继续编辑和查询本次病历 |
| R-12 | 检查申请与执行 | 门诊/检查医生 | 检查开单、缴费、登记、执行、结果及正式报告 | D7-D8 | 已缴费申请进入检查队列，正式报告回写病历 |
| R-13 | 检验申请与执行 | 门诊/检验医生 | 检验开单、标本登记、指标录入、异常标记和报告 | D7-D8 | 检验结果包含单位、参考范围和异常标记 |
| R-14 | 处置申请与执行 | 门诊/处置医生 | 处置开单、缴费、执行过程和结果记录 | D7-D8 | 处置记录回写病历并可供门诊医生查看 |
| R-15 | AI 分诊台 | 医技医生、患者 | 根据急诊、队列、设备和地点进行资源分配 | AI-D3/D4，集成 D7 | 已缴费医嘱获得执行地点和队列，过号进入队尾 |
| R-16 | CT 影像识别 | 检查医生 | 影像上传、异步推理、异常区域、风险提示 | AI-D4，集成 D8 | AI 结果不可直接发布，必须由检查医生确认 |
| R-17 | AI 检查报告 | 检查/检验医生 | 根据检查数据生成报告草稿并保留人工修改记录 | AI-D4，集成 D8 | 正式报告具有确认医生和确认时间 |
| R-18 | AI 助理医生 | 门诊医生 | 病情摘要、诊断方向、检查建议、用药建议和风险提醒 | AI-D3，集成 D8-D9 | AI 建议与最终诊断、处方分开存储并需人工确认 |
| R-19 | 处方与缴费 | 门诊医生、患者、窗口人员 | 处方开具、药品明细、费用生成、线上/窗口缴费 | D8-D9 | 只有医生确认且已缴费处方可进入药房 |
| R-20 | 发药、退药与库存 | 药房医生 | 待发药、发药、退药、库存扣减和回补 | D9 | 发退药记录、操作人、时间和库存流水完整 |
| R-21 | AI 排班 | 管理员 | 根据请假、科室需求和历史挂号量生成排班建议 | AI-D5，集成 D9 | 管理员确认后才能发布并影响号源 |
| R-22 | 日志与审计 | 管理员 | 登录、病历访问、支付状态、AI 调用和人工确认日志 | D2-D10 | 关键操作可以按用户、患者、业务单号追溯 |

## 4. 主系统两周详细计划

### 4.1 第一周：基础平台、账号、排班与挂号收费闭环

#### D1：需求冻结、架构和接口契约

**对应需求：** R-01 至 R-22 的公共基础。

**任务：**

1. 盘点现有 PC Web、后端服务、数据库迁移和 AI 占位代码，建立“保留、重构、新增、淘汰”清单。
2. 确认 P0、P1 和不纳入范围，冻结 MVP。
3. 确定患者、窗口人员、门诊医生、检查医生、检验医生、处置医生、药房医生和管理员权限矩阵。
4. 定义核心状态机：
   - 号源：可用、锁定、已占用、已释放。
   - 挂号：待支付、已挂号、已取消、已退号、已完成。
   - 支付：待支付、已支付、支付失败、退款中、已退款。
   - 就诊：待诊、接诊中、过号、复诊等待、已结束、已归档。
   - 医嘱：已开立、待缴费、待执行、执行中、已完成、已取消。
   - 报告：草稿、待确认、已确认、已驳回。
   - 处方：草稿、已确认、待缴费、待发药、已发药、已退药。
5. 对照现有 Flyway V1 迁移完成目标 ER 图和增量迁移设计。
6. 对照现有接口冻结保留接口、变更接口、废弃接口、AI API、错误码、分页和统一返回结构。
7. 明确事件同步规则：挂号成功、缴费成功、退款成功、检查完成、报告确认、处方确认和发药完成。

**核心数据表：**

`patient`、`staff_user`、`role`、`permission`、`department`、`doctor`、`doctor_schedule`、`schedule_slot`、`appointment`、`payment_order`、`medical_record`、`medical_order`、`report`、`prescription`、`drug_inventory`、`audit_log`、`ai_task`。

**交付物：**

- MVP 功能清单。
- 现有代码差距与改造清单。
- 权限矩阵。
- ER 图与数据库迁移脚本初稿。
- OpenAPI 初稿。
- 状态机和事件清单。

**验收标准：**

- 所有角色、流程状态和数据责任人不存在歧义。
- 主系统与 AI 团队基于同一份接口契约开发。

#### D2：工程搭建、认证、权限和基础设施

**对应需求：** R-01、R-03、R-22。

**任务：**

1. 保留现有 `frontend/` 作为医院工作人员 PC Web，建立患者页面迁出和工作人员角色拆分方案。
2. 新建患者微信小程序工程，复用现有 API 类型和业务规则，不复用 Element Plus 页面。
3. 保留现有 Maven 多模块结构和 FastAPI 工程，补齐统一异常、参数校验、日志和审计字段。
4. 通过新增 Flyway 迁移升级账号和权限表，不覆盖已有 V1 数据库迁移。
5. 将明文密码改为 BCrypt/Argon2 哈希，将开发 Token 改为可验证、可过期的访问 Token。
6. 将 `PATIENT/DOCTOR/ADMIN` 改为患者、窗口人员、门诊医生、检查医生、检验医生、处置医生、药房医生和管理员角色。
7. 修改 Spring Security，由全部放行改为仅登录、注册、健康检查等白名单接口公开。
8. 部署并验证现有 PostgreSQL/pgvector、Redis、MinIO 和开发环境反向代理。
9. 建立 CI 基础检查：前端构建、后端测试、Python 语法/测试和代码格式检查。

**交付物：**

- 可启动的患者微信小程序、工作人员 PC Web、后端和 AI 四类工程。
- Docker Compose 开发环境。
- 安全登录、Token 校验及细分权限接口。
- 数据库增量迁移脚本。

**验收标准：**

- 各角色登录后进入正确工作台。
- 未授权接口返回 401/403。
- 数据库中不存在新增的明文密码，过期或伪造 Token 无法访问受保护接口。
- 关键登录行为写入审计日志。

#### D3：患者、实名认证、基础数据、医生排班与号源

**对应需求：** R-01、R-02、R-04。

**任务：**

1. 微信小程序实现手机号注册、验证码登录、密码登录和找回密码。
2. 实现患者实名认证及实名状态拦截。
3. 管理端实现科室、医生、医生类型、挂号级别和结算类别维护。
4. 实现医生排班、时段、号源数量、停诊和调班。
5. 实现微信小程序与窗口工作台共用的排班、医生和号源查询 API。
6. 准备门诊科室、医生、药品及检查项目演示数据。

**交付物：**

- 患者账号及实名认证页面。
- 管理端基础数据和排班页面。
- 统一排班和号源查询接口。

**验收标准：**

- 未实名认证患者不能挂号或缴费。
- 排班发布后，小程序和窗口查询结果一致。
- 停诊后对应号源不可继续预约。

#### D4：线上挂号、窗口挂号和支付订单

**对应需求：** R-07、R-08。

**任务：**

1. 复用现有挂号领域对象和接口，移除构造器中的演示数据初始化逻辑。
2. 将“记录总数 + 1”ID 改为 UUID/序列和独立可读业务编号。
3. 微信小程序实现按科室、医生、日期和时段查询号源。
4. 将现有 JVM `synchronized` 锁号改为 PostgreSQL 行锁/原子更新，实现锁定超时和并发校验。
5. 实现线上挂号确认和模拟/沙箱支付，以 0.01 元完成订单、回调幂等、支付流水和业务状态联动；生产配置关闭模拟入口。
6. PC Web 实现窗口患者查询、线下建档和现场挂号。
7. 修复线下挂号不扣减统一号源的问题，使线上与窗口执行相同库存事务。
8. 新增统一支付订单，支持挂号费、检查费、检验费、处置费和处方费。
9. 窗口收费结果与线上支付使用相同支付状态模型。

**交付物：**

- 小程序挂号与支付页面。
- 窗口挂号收费工作台。
- 统一号源锁和支付订单服务。

**验收标准：**

- 同一号源不能被线上和窗口重复占用。
- 支付失败或超时后自动释放号源。
- 线上与线下记录可通过同一业务编号查询。
- 多实例或并发请求下，号源的 `locked + booked` 始终不超过容量。

#### D5：退号退费、电子病历创建和统一门诊队列

**对应需求：** R-07、R-08、R-09、R-10。

**任务：**

1. 将现有同步调用且静默忽略异常的病历创建方式改为本地事件表/可靠消息与幂等消费。
2. 实现挂号成功后自动创建本次电子病历，并能查询待补偿或失败事件。
3. 实现线上和线下患者进入同一门诊队列。
4. 新增退款记录，实现取消挂号、退号、退费和号源回补。
5. 落实线上取消规则：就诊日期前一天及以前允许取消，当天不可线上取消。
6. 实现门诊叫号、开始接诊和过号顺延 3 个号。
7. 为号源并发、重复支付、重复取消和病历幂等创建补充自动化测试。
8. 完成第一周接口集成测试和演示流程。

**第一周里程碑：**

```text
微信小程序线上挂号
或 PC Web 窗口线下挂号
→ 统一缴费状态
→ 统一扣减号源
→ 自动创建电子病历
→ 进入同一门诊队列
```

**验收标准：**

- 线上和线下挂号均能进入正确医生队列。
- 退号退费后号源、支付和挂号状态一致。
- 门诊过号后患者准确顺延 3 个号。

### 4.2 第二周：接诊、医技、报告、处方、药房和整体验收

#### D6：门诊医生接诊与电子病历

**对应需求：** R-05、R-06、R-09、R-10、R-11。

**任务：**

1. 实现门诊医生今日队列、患者搜索和开始接诊。
2. 实现病历首页：主诉、现病史、既往史、过敏史、体格检查和初步诊断。
3. 接入 AI 问诊摘要和医生推荐结果。
4. 实现本次病历、相关历史病历和访问日志。
5. 实现病历保存、版本记录和并发更新控制。
6. 门诊医生工作台采用左右分屏，左侧完成队列/病历/医嘱/处方操作，右侧常驻 AI 对话与建议，支持将 AI 草稿填入左侧后人工复核。
7. 诊断草稿记录 `created_by_type`、AI 记录 ID、确认医生和确认时间。

**验收标准：**

- 医生可查看患者 AI 问诊内容并进行更正。
- 默认只加载本次或相关病历，查看其他历史病历需记录审计日志。
- 病历未保存时不能结束接诊。

#### D7：检查、检验、处置开单与 AI 分诊

**对应需求：** R-12、R-13、R-14、R-15。

**任务：**

1. 实现医技项目分类、搜索、常用项目和批量选择。
2. 门诊医生开具检查、检验和处置申请。
3. 生成费用订单，支付成功后医嘱进入待执行状态。
4. 建立检查、检验和处置工作台及各自队列。
5. 接入 AI 分诊结果，综合医生擅长方向、当前负载、急诊等级、地点和设备，分配到具体执行医生、设备和队列。
6. 实现急诊优先及医技过号进入队尾规则。
7. 检验医嘱建立样本条码和 `REQUESTED → COLLECTED → RECEIVED → ANALYZING → REVIEWED → EXHAUSTED/DISCARDED` 状态机。
8. 检验结果按指标纵向 Key-Value 明细存储，禁止为血糖、血红蛋白等指标增加固定宽表列。
9. AI 生成的报告或检验摘要必须关联 AI 记录，并由对应医生确认后进入正式状态。

**验收标准：**

- 未缴费医嘱不能执行。
- 不同类型申请进入正确工作台。
- 执行人员只能处理授权科室和项目。

#### D8：结果录入、CT 影像、正式报告与复诊

**对应需求：** R-12 至 R-18。

**任务：**

1. 检查医生录入检查所见、检查结论和附件。
2. 检验医生录入指标、单位、参考范围和异常标记。
3. 处置医生记录执行过程、结果和后续建议。
4. 实现 CT/DICOM 上传至 MinIO，业务元数据写入 PostgreSQL。
5. 接入 AI CT 识别和报告草稿异步任务。
6. 检查医生确认、修改或驳回 AI 结果并发布正式报告。
7. 正式报告同步到门诊工作台、小程序和电子病历。
8. 患者完成医技项目后进入复诊队列。

**验收标准：**

- AI 草稿不能被患者或门诊医生当作正式报告查看。
- 正式报告包含确认医生、确认时间和 AI 修改痕迹。
- 报告发布后相关医嘱进入已完成状态。

#### D9：AI 辅助诊断、处方、药房和排班集成

**对应需求：** R-18、R-19、R-20、R-21。

**任务：**

1. 接入 AI 病情摘要、诊断方向、进一步检查建议、用药建议和风险提醒。
2. 实现医生采纳、修改、拒绝 AI 建议的记录。
3. 实现最终诊断、处方开具、药品剂量、用法和注意事项。
4. 实现处方支付和药房待发药队列。
5. 实现药房医生发药、退药、库存扣减、回补和库存流水。
6. 接入 AI 排班建议，由管理员确认后发布并同步号源。

**验收标准：**

- AI 不能直接生成最终诊断或生效处方。
- 未缴费处方不会进入待发药状态。
- 发药和退药后库存、处方、病历和取药状态保持一致。

#### D10：全流程测试、修复、部署和验收

**对应需求：** 全部 P0 需求。

**任务：**

1. 执行完整端到端测试：
   - 微信小程序线上挂号流程。
   - 窗口线下挂号和缴费流程。
   - 检查、检验和处置流程。
   - CT AI 报告确认流程。
   - 处方缴费和药房发药流程。
2. 执行异常测试：重复支付、重复挂号、支付超时、号源竞争、AI 超时、报告驳回和库存不足。
3. 执行权限和越权访问测试。
4. 校验业务状态、数据库约束、审计日志和 AI 调用记录。
5. 完成部署脚本、演示数据、用户操作说明和接口文档。
6. 修复阻断级和严重级缺陷，完成最终演示。

**最终交付物：**

- 患者微信小程序。
- 医院工作人员 PC Web。
- Spring Boot 业务后端。
- FastAPI AI 服务。
- PostgreSQL/pgvector 数据库迁移和初始化脚本。
- Docker Compose 部署文件。
- OpenAPI 接口文档。
- 测试报告、演示数据和操作手册。

## 5. AI 并行一周详细计划

AI 团队在第一周并行开发，主系统团队从 D1 起使用 Mock API 开发。AI 服务在 AI-D5 交付，主系统于 D6-D9 完成真实服务集成。

### AI-D1：AI 契约、服务骨架和安全边界

**对应需求：** R-05、R-06、R-15、R-16、R-17、R-18、R-21、R-22。

**任务：**

1. 创建 FastAPI 服务、统一认证、异常处理和 OpenAPI 文档。
2. 定义 AI 任务、会话、模型版本、提示词版本和人工确认字段。
3. 确定同步与异步接口：问诊使用流式/同步响应，CT 推理使用异步任务。
4. 为全部 AI 接口提供固定结构的 Mock 返回。
5. 制定安全规则：紧急风险提示、医疗免责声明、敏感信息脱敏、超时和失败降级。

**交付物：** AI OpenAPI、Mock 服务、数据结构和错误码。

### AI-D2：患者 AI 智能问诊

**对应需求：** R-05。

**任务：**

1. 解析症状标签、持续时间和自由文本。
2. 实现多轮追问和会话状态保存。
3. 生成症状摘要、风险等级、健康建议、就诊建议和推荐科室。
4. 建立紧急症状规则，紧急风险优先输出急诊或急救提示。
5. 将问诊结果以结构化 JSON 返回并保存调用记录。

**验收标准：**

- 信息不足时能够继续追问。
- 每次输出包含风险等级和安全提示。
- AI 服务异常时返回可识别的降级结果，不阻断患者自主挂号。

### AI-D3：医生推荐、AI 分诊和医生辅助诊断

**对应需求：** R-06、R-15、R-18。

**任务：**

1. 根据症状和医生擅长方向计算匹配度。
2. 结合排班、剩余号源、职称和风险等级推荐 3-5 名医生。
3. 根据急诊优先、队列长度、设备状态和项目类型生成医技分诊建议。
4. 根据本次病历和正式报告生成病情摘要、诊断方向、检查建议、用药建议和风险提醒。
5. 使用 PostgreSQL + pgvector 实现医学知识和医生擅长方向检索。

**验收标准：**

- 推荐医生必须存在有效排班和剩余号源。
- AI 诊断和用药建议标记为待医生确认。
- 只接收授权范围内的当前病历和相关历史病历。

### AI-D4：CT 影像识别和智能报告草稿

**对应需求：** R-16、R-17。

**任务：**

1. 实现 CT/DICOM 文件读取、预处理和推理任务。
2. 输出疑似异常区域、置信度、风险提示和模型版本。
3. 根据影像结果生成结构化检查报告草稿。
4. 实现任务排队、进度查询、失败重试和超时状态。
5. 保存 AI 原始输出，支持检查医生确认、修改和驳回。

**验收标准：**

- 上传后立即返回任务 ID。
- 推理失败不会生成正式报告。
- AI 结果与正式报告物理或逻辑分离。

### AI-D5：AI 排班、测试、容器化和交付

**对应需求：** R-21、R-22。

**任务：**

1. 根据医生工作制度、请假、调休、科室需求和历史挂号量生成排班建议。
2. 完成所有 AI 接口单元测试、契约测试和基础性能测试。
3. 验证提示词注入防护、敏感信息处理和越权数据访问限制。
4. 完成 Docker 镜像、环境变量说明和模型文件挂载说明。
5. 向主系统团队交付部署包、OpenAPI、示例请求、错误码和测试报告。

**AI 一周交付标准：**

- 所有 P0 AI API 可独立调用。
- AI 服务不可用时主业务仍可人工完成。
- AI 输入、输出、模型版本、提示词版本和人工确认状态可追溯。
- CT 等长任务支持异步执行和状态查询。

## 6. 主系统与 AI 接口清单

| 接口 | 调用方 | 调用时机 | 响应方式 | 失败处理 |
|---|---|---|---|---|
| `POST /ai/consultations` | 微信小程序 | 创建问诊会话 | 同步 | 提示暂不可用，允许自主挂号 |
| `POST /ai/consultations/{id}/messages` | 微信小程序 | 提交症状或回答追问 | SSE/同步 | 保存患者原始描述并允许重试 |
| `POST /ai/doctor-recommendations` | 微信小程序 | 问诊结束或主动推荐 | 同步 | 展示科室和医生搜索列表 |
| `POST /ai/triage` | 业务后端 | 医技项目缴费成功 | 同步 | 使用规则分诊和队列最短策略 |
| `POST /ai/clinical-assistance` | 门诊 PC Web | 复诊或请求 AI 建议 | SSE/同步 | 医生继续人工诊断 |
| `POST /ai/ct-analysis` | 检查 PC Web | CT 文件上传完成 | 异步 | 标记任务失败，允许重新提交 |
| `GET /ai/tasks/{id}` | 业务后端/PC Web | 查询推理状态 | 同步 | 显示失败原因和重试入口 |
| `POST /ai/report-drafts` | 业务后端 | 影像或检查结果完成 | 异步/同步 | 检查医生人工填写报告 |
| `POST /ai/schedule-suggestions` | 管理 PC Web | 管理员生成排班建议 | 同步 | 管理员人工排班 |

## 7. 数据与状态一致性要求

### 7.1 统一数据源

- PostgreSQL 是挂号、缴费、病历、医嘱、报告、处方、库存和排班的事实数据源。
- Redis 只保存缓存、号源临时锁、验证码和短期队列数据，不作为最终业务依据。
- MinIO 保存影像和报告附件，PostgreSQL 保存文件元数据及业务关联。
- pgvector 保存向量字段和索引，原始病历仍以 PostgreSQL 业务表为准。

### 7.2 必须保证原子性的操作

1. 挂号成功、号源扣减和病历创建。
2. 退号成功、退款记录和号源回补。
3. 缴费成功和医嘱状态变更。
4. 报告确认和电子病历更新。
5. 发药成功、库存扣减和处方状态变更。
6. 退药成功、库存回补和退药流水创建。

### 7.3 AI 数据边界

- AI 原始结果和医生最终结果分开保存。
- 关键医疗数据强制保存 `created_by_type(HUMAN/AI)`；AI 来源必须关联 AI 记录 ID，正式化时必须保存 `confirmed_by` 和 `confirmed_at`。
- AI 只读取授权的本次病历、相关历史病历和正式报告。
- AI 不直接改变诊断、正式报告、处方或排班状态。
- 每次 AI 调用记录调用人、患者、输入范围、模型版本、输出和耗时。

## 8. 测试与验收计划

### 8.1 核心验收场景

| 场景 | 验收步骤 | 预期结果 |
|---|---|---|
| 线上完整诊疗 | 小程序注册→问诊→挂号→支付→接诊→检查→报告→处方→支付→发药 | 全流程状态连续，病历完整归档 |
| 线下完整诊疗 | 窗口建档→挂号收费→接诊→窗口缴费→检查→处方→窗口缴费→发药 | 与线上共享号源、队列、支付及病历 |
| 号源并发 | 两名用户同时选择最后一个号源 | 仅一人成功锁定和支付 |
| 取消挂号 | 就诊前一天取消；就诊当天尝试线上取消 | 前者退费并回补号源，后者被规则拦截 |
| 门诊过号 | 医生将未到患者标记过号 | 患者顺延 3 个号重新进入队列 |
| 医技过号 | 检查患者未到并被标记过号 | 患者进入当前队列最后 |
| AI 报告确认 | AI 生成 CT 报告草稿，检查医生修改并确认 | 患者只能看到确认后的正式报告 |
| 0.01 元支付 | 创建测试订单并触发微信/支付宝沙箱或模拟回调，重复发送同一回调 | 仅生成一笔支付流水，业务状态只推进一次，交易标记为测试 |
| AI 分诊到医生 | 同一项目提供不同专长和负载的检查医生候选 | 优先选择专长匹配且负载合理的具体医生，并给出原因 |
| LIS 样本状态 | 采集、接收、分析、审核、用完；尝试跳跃状态和无原因废弃 | 正常流转成功，非法流转被拒绝并保留操作记录 |
| 检验明细扩展 | 增加新的检验指标 | 只增加指标字典/明细行，不修改业务表结构 |
| AI 来源追溯 | AI 生成诊断或检验摘要后尝试直接正式提交 | 缺少 AI 记录 ID 或人工确认时拒绝提交 |
| AI 故障降级 | 停止 AI 服务后继续挂号、接诊和报告录入 | 主业务正常，页面提示人工处理 |
| 药房库存 | 发药、重复发药、退药和库存不足 | 防止重复操作，库存流水正确 |
| 权限隔离 | 不同角色访问他人接口或病历 | 越权操作被拒绝并记录日志 |

### 8.2 缺陷放行标准

- 阻断级缺陷：必须为 0。
- 严重级缺陷：必须为 0。
- 一般缺陷：不影响主流程，可记录到后续迭代。
- 所有 P0 验收场景必须通过。
- 线上和线下业务数据一致性检查必须通过。

## 9. 每日协作和进度控制

### 9.1 每日节奏

1. 每日开始：15 分钟同步昨日结果、当日目标和阻塞项。
2. 每日中午：更新接口和数据库变更，避免主系统与 AI 契约漂移。
3. 每日结束：部署到共享测试环境，记录已完成、未完成和缺陷。
4. D5 和 D10：执行里程碑验收，不通过的 P0 功能优先于新增功能。

### 9.2 完成定义

一项任务只有同时满足以下条件才算完成：

- 代码已合并并可编译部署。
- 数据库迁移脚本已提交。
- API 文档已更新。
- 正常与异常路径测试通过。
- 权限和审计要求已验证。
- 页面可以在共享环境完成业务操作。

## 10. 主要风险与应对措施

| 风险 | 影响 | 应对措施 |
|---|---|---|
| 两周内微服务拆分过多 | 联调和部署成本失控 | 采用模块化单体完成 MVP，按既定边界组织模块 |
| 微信支付或短信服务未开通 | 挂号支付流程阻塞 | 使用适配器和沙箱/模拟实现，保留正式渠道接口 |
| CT 模型或数据不可用 | 无法演示影像识别 | 使用现有模型或固定样例推理结果，保证人工报告流程可用 |
| AI 输出不稳定 | 医疗建议不可控 | 结构化输出、规则校验、人工确认和失败降级 |
| 线上与窗口争抢号源 | 重复挂号 | Redis 锁加 PostgreSQL 唯一约束和事务校验 |
| 多模块状态不一致 | 患者无法继续下一环节 | 统一状态机、事务消息或本地事件表、幂等接口 |
| 需求持续扩大 | 无法按期完成 | 严格执行 P0/P1，新增需求进入下一迭代 |
| 测试时间被开发挤占 | 最终主流程不稳定 | D5 强制完成第一轮集成，D10 仅修复和验收 |

## 11. 最终验收里程碑

| 里程碑 | 时间 | 通过条件 |
|---|---|---|
| M1：方案冻结 | D1 | 权限、ER 图、状态机和 API 契约确认 |
| M2：基础平台可用 | D3 | 小程序及 PC Web 可登录，排班号源可查询 |
| M3：挂号收费闭环 | D5 | 线上和线下挂号进入统一队列 |
| M4：AI 服务交付 | AI-D5 | AI API 独立部署并通过契约测试 |
| M5：诊疗医技闭环 | D8 | 接诊、医技执行、正式报告和复诊贯通 |
| M6：处方药房闭环 | D9 | 诊断、处方、缴费、发药和库存贯通 |
| M7：项目验收 | D10 | 全部 P0 场景通过，可部署、可演示、可追溯 |
